/** @jsxImportSource @emotion/react */

import MainCard from "../components/Main/MainCard";

const Home = () => {
  return (
    <div
      css={{
        display: "flex",
        flexDirection: "column",
        width: "100%",
      }}
    >
      <MainCard />
    </div>
  );
};

export default Home;
